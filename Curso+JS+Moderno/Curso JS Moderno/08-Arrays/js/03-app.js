const meses = ['enero','febrero','marzo']

console.table(meses);

// recorrer un arreglo 
// para ello necesitamos el tamao del arreblo

console.log(meses.length);// método para ver el tamaño del arreglo

for (let index = 0; index < meses.length; index++) {
    console.log(meses[index]);
    const element = meses[index];
    
}