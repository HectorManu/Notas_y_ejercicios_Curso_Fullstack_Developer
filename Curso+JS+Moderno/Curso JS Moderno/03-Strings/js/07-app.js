const producto = 'Monitor 20 Pulgadas';
console.log(producto.toUpperCase());// ESto es para volver todo a mayusculas

console.log(producto.toLowerCase()); //lo pasamos a minusculas

const email = "CORREO@CORREO.COM"

console.log(email.toLowerCase()); //lo pasamos a minusculas

const precio = 300;

// Hacesuna conversión de de entero a String
console.log(precio.toString());
